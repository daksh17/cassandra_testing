# Generated — ClusterIP (mongo-config2)
apiVersion: v1
kind: Service
metadata:
  name: mongo-config2
  namespace: demo-hub
  labels:
    app.kubernetes.io/name: mongo-config2
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: mongo-config2
  ports:
    - port: 27017
      targetPort: 27017
      name: p-27017

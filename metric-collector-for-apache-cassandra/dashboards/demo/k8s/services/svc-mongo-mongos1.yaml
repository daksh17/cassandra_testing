# Generated — ClusterIP (mongo-mongos1)
apiVersion: v1
kind: Service
metadata:
  name: mongo-mongos1
  namespace: demo-hub
  labels:
    app.kubernetes.io/name: mongo-mongos1
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: mongo-mongos1
  ports:
    - port: 27017
      targetPort: 27017
      name: p-27017

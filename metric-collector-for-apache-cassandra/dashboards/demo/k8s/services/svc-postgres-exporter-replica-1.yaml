# Generated — ClusterIP (postgres-exporter-replica-1)
apiVersion: v1
kind: Service
metadata:
  name: postgres-exporter-replica-1
  namespace: demo-hub
  labels:
    app.kubernetes.io/name: postgres-exporter-replica-1
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: postgres-exporter-replica-1
  ports:
    - port: 9187
      targetPort: 9187
      name: p-9187

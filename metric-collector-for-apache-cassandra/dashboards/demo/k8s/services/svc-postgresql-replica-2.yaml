# Generated — ClusterIP (postgresql-replica-2)
apiVersion: v1
kind: Service
metadata:
  name: postgresql-replica-2
  namespace: demo-hub
  labels:
    app.kubernetes.io/name: postgresql-replica-2
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: postgresql-replica-2
  ports:
    - port: 5432
      targetPort: 5432
      name: p-5432

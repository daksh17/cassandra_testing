# Generated — ClusterIP (mongo-mongos3)
apiVersion: v1
kind: Service
metadata:
  name: mongo-mongos3
  namespace: demo-hub
  labels:
    app.kubernetes.io/name: mongo-mongos3
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: mongo-mongos3
  ports:
    - port: 27017
      targetPort: 27017
      name: p-27017

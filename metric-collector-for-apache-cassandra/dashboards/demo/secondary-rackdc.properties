# Required by GossipingPropertyFileSnitch. Default for Secondary cluster (datacenter2/rack2).
# Override per-node via CASSANDRA_DC / CASSANDRA_RACK in docker-compose if needed.
dc= datacenter2
rack= rack2

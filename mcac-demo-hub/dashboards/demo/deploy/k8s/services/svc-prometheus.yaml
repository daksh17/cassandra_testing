# Generated — ClusterIP (prometheus)
apiVersion: v1
kind: Service
metadata:
  name: prometheus
  namespace: demo-hub
  labels:
    app.kubernetes.io/name: prometheus
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: prometheus
  ports:
    - port: 9090
      targetPort: 9090
      name: p-9090

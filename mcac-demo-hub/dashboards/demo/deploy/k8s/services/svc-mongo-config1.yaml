# Generated — ClusterIP (mongo-config1)
apiVersion: v1
kind: Service
metadata:
  name: mongo-config1
  namespace: demo-hub
  labels:
    app.kubernetes.io/name: mongo-config1
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: mongo-config1
  ports:
    - port: 27017
      targetPort: 27017
      name: p-27017
